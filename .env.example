# Copy to .env (git-ignored) to override docker-compose defaults.

# SQL Server 2022 is amd64-only. On ARM64 hosts use Azure SQL Edge (engine-compatible for this repo):
# SQL_IMAGE=mcr.microsoft.com/azure-sql-edge:latest

# SQL_PORT=1433
# MSSQL_SA_PASSWORD=FeeBilling!Passw0rd
